\tikzstyle{greyBlock}=[
    % shape
    rectangle,
    rounded corners=1mm,
    minimum size=6mm,
    %border
    very thick,
    font=\normalsize,
    draw=black!50,
    fill=white,
    % align is needed to allow linebreaks inside a node
    align=center
]            
\tikzstyle{blueBlock}=[
    % shape
    rectangle,
    rounded corners=1mm,
    minimum size=6mm,
    %border
    very thick,
    font=\normalsize,
    draw=blue!75,
    fill=white,
    align=center
]
\tikzstyle{dummy}=[
    % shape
    rectangle,
    rounded corners=1mm,
    minimum size=6mm,
    %border
    thick,
]
% Define arrow style
\tikzstyle{arrow}=[
   draw,
   >=stealth',	% arrow tip
   ->,
   thick,
   shorten <=2pt,
   shorten >=2pt
]
% define line style
\tikzstyle{line}=[
    draw,
    thick,
    text centered,
    shorten <=2pt,
    shorten >=2pt
]
\tikzstyle{line node}=[
    draw,
    fill=white,
    font=\tiny
]

\tikzstyle{classNode}=[
    % shape
    rectangle,
    rounded corners=1mm,
    minimum size=6mm,
    %border
    very thick,
    font=\normalsize\bfseries,
    %draw=black!50,
    %fill=white,
    % align is needed to allow linebreaks inside a node
    align=center
]      
\tikzstyle{stringNode}=[
    % shape
    rectangle,
    rounded corners=1mm,
    minimum size=6mm,
    %border
    very thick,
    font=\normalsize\itshape,
    %draw=black!50,
    %fill=white,
    % align is needed to allow linebreaks inside a node
    align=center
]      